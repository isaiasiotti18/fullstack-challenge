{
  "name": "@crash/e2e",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "seed": "bun run src/helpers/seed.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/pg": "^8.20.0",
    "pg": "^8.20.0"
  }
}
